# mui

> Write once, deploy everywhere.

A cross-platform UI abstraction for Haxe. Build native apps for macOS/iOS, Windows, Android, and terminal from a single codebase.

- Unified API across `sui` (SwiftUI), `wui` (WinUI), `aui` (Compose), and `cui` (TUI)
- Zero runtime overhead via compile-time backend selection
- Type-safe bindings with `@:from` abstracts
- Macro-based `ForEach` that works across codegen and runtime backends

[GitHub](https://github.com/lapavoiserie/mui ':target=_blank')
[Get Started](getting-started.md)
