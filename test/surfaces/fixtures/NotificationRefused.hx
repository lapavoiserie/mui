import mui.App;
import mui.View;
import mui.ui.Text;

/** No declaration form until the detached subsystem brings its contract. **/
class NotificationRefused extends App {
	override function body():View return new Text("body");

	@:surface(Notification)
	function ping():View return new Text("no");

	static function main() {}
}
